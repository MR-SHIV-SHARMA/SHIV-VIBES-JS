/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com', 'aceternity.com', 'images.pexels.com', 'avatars.githubusercontent.com'
        ]
    }
};

export default nextConfig;
